//- This is how we extend a template using the 'extends' keyword
extends ../../../templates/master.pug

//- This is how we insert dynamic content to extended templates
block content
    hr
    h5.title List of all cars
    <a>
        ul
            //- This is how we iterate through arrays and access
            //- their elements
            each vehicle in vehicles
                a(href=`/users/\`${vehicle.id}\``)
                    li= `${vehicle.id} - ${vehicle.maker} - ${vehicle.model} - ${vehicle.year}`
    br
    a.button.is-black(href="/users/new") 
        span Add New Car
        